# typeset 設計（Phase 0〜2 の具体化）

`検討.md` で決めた方針（新規＋richtext からの移植・一般化、glyphware 直結、ICU 非依存、表示リストで backend を切る）を、
ディレクトリ・名前空間・型・処理の流れに落とす。ターゲットは **台本・ライトノベル・技術文書**。
吹き出しは将来（ただし回り込みに必要な「行ごとの行長」機構は最初から入れる）。表は TeX の tabular 水準を目標にする（Phase 3）。

---

## 1. リポジトリ構成

```
typeset/
  CMakeLists.txt  CMakePresets.json  Makefile  vcpkg.json      richtext / glyphware と同じ流儀
  ext/glyphware/            git submodule（github.com/wamsoft/glyphware）
  include/typeset/
    geom.hpp                Pt, Point, Size, Rect, Matrix, Path
    writing_mode.hpp        WritingMode / TextOrientation、論理座標 ↔ 物理座標
    text/                   Unicode・JLReq のテーブルと判定（CharClass, SpacingTable, UAX#50, EAW, 行分割機会）
    font/                   FontSet（glyphware Registry の薄い包み）, FontSpec, 縦メトリクス合成
    style.hpp               TextStyle / ParagraphStyle / BlockStyle
    inline/                 Itemizer, Shaper, ItemBuilder, LineBreaker, LinePlacer, ParagraphLayouter
    block/                  Block（段落・見出し・画像・罫線・表・ラベル付き段落）, Flow, FlowLayouter
    page/                   PageMaster, PageSequence, Region, 柱・ノンブル
    dl/                     DisplayList と要素型
    backend/                RasterRenderer, PdfWriter, SvgWriter
    doc/                    Document（フォント・マスタ・フロー・出力をまとめる入口）
    markup/                 インラインマークアップ（任意。Phase 2 以降）
  src/                      同じ階層で .cpp
  python/                   pybind11 モジュール（psdparse 準拠）、pyproject.toml はルート
  samples/                  sample_dl / sample_inline / sample_page / sample_script（台本）/ sample_novel
  tests/                    doctest（vcpkg）。組版結果は数値で検算する（禁則違反 0 件、行長一致 等）
  data/                     フォント取得スクリプト（richtext の download_fonts.py を流用）
  README.md 検討.md 設計.md 実装.md
```

名前空間は `typeset::` 直下に `text` / `font` / `inl`（inline は予約語）/ `block` / `page` / `dl` / `backend` / `doc`。

外部依存（vcpkg）: freetype, harfbuzz（typeset 自身も直接リンク。`hb_unicode_*` と hb-subset を使う）, libunibreak, zlib, libpng, stb（画像読み込み）, doctest, pybind11（Python 有効時）。
SheenBidi / picojson は glyphware が FetchContent で取る。**minikin・ICU は無い。**

---

## 2. 基本の型

### 2.1 単位と幾何（`geom.hpp`）

```cpp
using Pt = float;                        // 1/72 inch。文書内部は全部 pt
constexpr Pt mm = 72.0f / 25.4f;
constexpr Pt inch = 72.0f;
struct Point { Pt x, y; };  struct Size { Pt w, h; };  struct Rect { Pt x, y, w, h; };
struct Matrix { float a, b, c, d, e, f; };   // x' = a x + c y + e,  y' = b x + d y + f（PDF と同じ並び、y-down で使う）
struct Path { enum Cmd { Move, Line, Quad, Cubic, Close }; std::vector<Cmd> cmds; std::vector<Point> pts; };
struct Color { uint8_t r, g, b, a; };
```

ラスタ backend は `dpi / 72` を掛けて描く。richtext の「1px = 1pt」は採らない。

### 2.2 書字方向と論理座標（`writing_mode.hpp`）

```cpp
enum class WritingMode { HorizontalTb, VerticalRl, VerticalLr };
enum class TextOrientation { Mixed, Upright, Sideways };

// 行内はこの 2 軸で組む。richtext の (u, v) を一般化したもの
struct LogicalPoint { Pt inline_; Pt block; };   // inline_: 行の進む方向（送り）, block: 行に直交（横組みなら下、縦組みなら左）
```

- 横組み: inline = +x、block = +y、行の基準線は**行の中心線**（後述 2.6）
- 縦組み(rl): inline = +y、block = −x
- `toPhysical(WritingMode, LogicalPoint, lineOrigin)` の 1 関数で写す。グリフの回転（横倒し −90°）は物理化の時に `dl::GlyphRun` の 2x2 に入る

### 2.3 フォント（`font/`）

```cpp
struct FontSpec {                         // スタイルに書く論理指定
    std::vector<std::string> family;      // フォールバック順。glyphware Registry の名前解決に渡す
    int weight = 400; bool italic = false; float width = 100;   // width は wdth 軸 or 横スケール
    std::vector<std::pair<uint32_t,float>> features;            // OpenType feature（palt/vpal 等）
};

struct FontDeclaration { std::string key, path; int faceIndex; std::vector<std::string> family; int weight; std::optional<bool> italic;
                         std::vector<std::string> languages; std::vector<glyphware::CodepointRange> ranges; };

class FontSet {                            // glyphware の薄い包み。文書に 1 つ
public:
    std::shared_ptr<glyphware::Face> loadFile(path, key, faceIndex);   // 即時に開く（family / weight / italic は name・OS/2 から）
    bool declare(FontDeclaration);                                     // メタデータだけ登録。初回使用時に開く（ranges があれば開かずにカバレッジ判定）
    std::shared_ptr<glyphware::Face> select(name, weight, italic);     // 同じ name の登録から CSS Fonts の規則で最近傍（斜体の一致が先）
    // FontSpec と必要文字から face を解決。languageFonts(language)（setLanguageFonts の表＋宣言の languages）の family を先に試す。
    // family ごとに weight / italic の最近傍から見て cp を持つ最初の face。無ければフェイクボールド／斜体は組版層で
    std::shared_ptr<glyphware::Face> resolve(const FontSpec&, char32_t cp, const std::string& language = {});
    std::shared_ptr<glyphware::Face> primary(const FontSpec&);          // 行のメトリクス基準（言語の置換は見ない）
    std::shared_ptr<glyphware::Face> instance(base, coords);            // バリアブル軸のインスタンスは別 Face（同じ blob）。(base, 座標) でキャッシュ
};
// FontSpec::variations（tag → 値）と weight → wght、italic → ital。TextStyle::features は hb_shape へ。palt 等の字幅 feature が付いた run は
// item_builder が JLReq の約物の詰めを使わない。PDF はサブセット時に軸を固定（hb_subset_input_pin_axis_location）
```

縦メトリクス（vmtx / VORG）が無いフォントは richtext と同じ合成（送り 1em、原点を ascender 分上へ）。判定は `hb_ot_metrics` / `FT_HAS_VERTICAL`。

### 2.4 スタイル（`style.hpp`）

```cpp
struct TextLayer { std::optional<Color> fill; std::optional<Stroke> stroke; Point offset; Pt blur; };  // 外観の 1 層
struct TextShadow { Color color; Point offset; Pt blur; };          // 層の一番下に置く糖衣
struct TextDecoration { std::optional<Color> color; Pt thickness; float offset; };  // 下線・打消し線

struct TextStyle {
    FontSpec font; Pt size = 10;
    Color fill{0,0,0,255}; std::optional<Stroke> stroke;      // 縁取りは台本の強調で使う
    std::optional<TextShadow> shadow;                          // 影
    std::vector<TextLayer> layers;                             // 空なら fill/stroke の 1 層。resolvedLayers() が shadow + layers を返す
    std::optional<TextDecoration> underline, strikethrough;    // 横組み: ベースライン下／x ハイト中。縦組み: 右側の傍線／中心線
    float letterSpacing = 0;                                   // em
    std::optional<TextOrientation> orientation;                // 部分的な正立／横倒し指定
    float scaleX = 1, scaleY = 1;                              // 平体・長体
};

struct ParagraphStyle {
    enum class Align { Start, End, Center, Justify } align = Align::Justify;
    Direction direction = Auto;     // 基底方向（UAX #9）
    float firstLineIndent = 1;      // em。日本語段落の一字下げ
    float hangingIndent = 0;        // em。2 行目以降の下げ（途中からの字下げは Annotation::indent）
    std::u16string ellipsis;        // 行数上限で切れたときの省略記号
    std::vector<TabStop> tabStops;  // 左／中央／右／小数点揃え。空なら tabWidth × em ごと
    Pt linePitch = 0;               // 行送り。0 なら size * lineHeight
    float lineHeight = 1.7f;
    text::SpacingOptions spacing;   // 約物詰め・和欧間・ぶら下げ・letterSpacing（richtext の VerticalSpacingOptions）
    inl::BreakOptions lineBreak;    // Greedy / KnuthPlass、tolerance
    Pt spaceBefore = 0, spaceAfter = 0;
    int orphans = 1, widows = 1;    // 段落の先頭・末尾に残す最小行数
    bool keepWithNext = false;      // 見出し用
    enum class Break { Auto, Column, Page } breakBefore = Break::Auto, breakAfter = Break::Auto;
};
```

### 2.5 表示リスト（`dl/`）

レイアウトの出力。ページごとに 1 本。座標は**ページ左上原点・y-down・pt**。

```cpp
struct GlyphRun {                          // 同じ face・サイズ・塗りの連続グリフ
    std::shared_ptr<glyphware::Face> face; Pt size;
    struct G { uint32_t gid; Point pos; Matrix2x2 xform; uint32_t charIndex; };   // xform: 回転・平体長体・フェイク斜体
    std::vector<G> glyphs;
    Color fill; std::optional<Stroke> stroke;
    Pt blur;                               // ぼかし半径（影の層）。ラスタ・SVG はガウスぼかし、PDF はぼかさない
    const std::u16string* sourceText;      // ToUnicode / SVG の代替テキスト用（所有はページ側）
};
// 双方向（UAX #9）: shapeText が SheenBidi で埋め込みレベルを求め、RTL セグメントは HB_DIRECTION_RTL で組んでクラスタを論理順に置く。
// 行を確定したあと reorderBidi（L2）が箱・グルーを視覚順に並べ替えて inline_ を付け直す。ParagraphStyle::direction が基底方向
// 取り出し口（inl/query.cpp）: charBoxes（注記を除く文字の箱。PlacedGlyph::boxBefore/boxAfter と em box）、rectsFor（範囲 → 行ごとの矩形）、
// placeholderRects、hitTest（行送りの箱 → 文字の箱。余白は行頭／行末に丸める）、caretRect、measureText（shapeText の包み）。
// emitParagraph(maxChars) は charIndex で描く文字を絞る（注記は親文字の charIndex を持つので一緒に出る）
// emitParagraph は TextStyle::resolvedLayers() の層ごとに GlyphRun を出す（下の層を行全体で先に。層の添字は最上層から揃える）。
// 下線・打消し線は RectItem（同じスタイルの連続グリフをひとつの線に。下線はグリフの前、打消し線はグリフの後）。
// 位置は ParagraphFragment::styleMetrics（第一候補フォントの post / OS/2 から hb_ot_metrics で取る）
struct PathItem  { Path path; std::optional<Color> fill; std::optional<Stroke> stroke; };
struct ImageItem { std::shared_ptr<Image> image; Matrix xform; std::optional<Rect> clip; };
struct RectItem  { Rect r; Color fill; };
struct Group     { Matrix xform; std::optional<Rect> clip; float opacity; std::vector<Item> children; };
struct Meta      { enum { Link, Bookmark, PageLabel, StructTag } kind; ... };
using Item = std::variant<GlyphRun, PathItem, ImageItem, RectItem, Group, Meta>;
struct DisplayList { Size pageSize; std::vector<Item> items; };
```

`GlyphRun::G::xform` の組み立ては richtext の `GlyphTransform.hpp`（フェイク幅／斜体 → スケール → 回転）をそのまま移す。
backend は 3 つとも同じ `DisplayList` を食う。差が出るのは「グリフをどう描くか」だけ:

| backend | グリフ | パス／矩形 | 画像 |
|---|---|---|---|
| Raster | glyphware `renderGlyphMask`（変形焼き込み、2x2＋1/4px 位相でキャッシュ）→ 合成（richtext `Raster.cpp` 移植） | 自前スキャンライン（AA）。まず矩形と直線、曲線は平坦化 | stb で読んでアフィン合成 |
| PDF | Identity-H、1 グリフ 1 `Tm`（richtext 移植）。Phase 6 で hb-subset・Flate | `m l c h f S` | `/XObject`（JPEG は DCTDecode 素通し、PNG は Flate） |
| SVG | `glyphOutline` を `<path d=…>` に。同じグリフは `<defs>` に 1 回置いて `<use>` | `<path>` / `<rect>` | `<image href="data:…">` |

### 2.6 行送りとブロック軸の位置

- 行送りは **固定ピッチ**（`linePitch` か `size × lineHeight`）。JLReq の「行の中心線」モデル。ルビや欧文が混ざっても行送りは変えない
- 行内のブロック軸位置は「**仮想ボディ（em box）の中心を行の中心線に合わせる**」を全書字方向で共通の規則にする
  - 和文正立: em box 中央＝中心線（縦組みなら列の中心線、横組みなら行の中心線）
  - 欧文（横組み）: フォントの ascender/descender から em box を置き、そのベースライン位置を求める（＝ CSS の ideographic-centered に近い）
  - 欧文横倒し（縦組み）: richtext と同じく ascent/descent の中点を列中心へ
  - ルビ・圏点: 親の em box の block 側（縦なら右、横なら上）に接して置く。割注は 2 行を em box 内に収める
- サイズ混在（`<font size>` やルビ）はこの規則から自然に決まる。ここを 1 箇所（`LinePlacer`）に閉じ込め、PDF/SVG/ラスタでずれないようにする

---

## 3. インライン組版（`inl/`）

richtext の縦組み経路を書字方向非依存にしたもの。処理は 6 段。

```
Paragraph（InlineRun 列＋注記）
  │ Itemizer      スタイル境界・スクリプト（hb_unicode_script）・フォント解決（FontSet::resolve）・向き（UAX#50 / orientation）で Run に分割
  │ Shaper        Run ごとに glyphware::shapeRun。正立は TTB（glyphware に追加）、それ以外は LTR / RTL。出力はクラスタ列
  │ ItemBuilder   クラスタ列＋注記 → Box / Glue / Penalty（richtext LineItemBuilder 移植。CharClass / SpacingTable もそのまま）
  │ LineBreaker   Greedy / Knuth–Plass。行長は LineShapeProvider から行ごとに取る
  │ LinePlacer    調整比でグルー確定 → 各クラスタの inline 位置、2.6 の規則で block 位置 → LineBox（論理座標）
  └ 物理化        LineBox を Region の位置と WritingMode で dl::GlyphRun へ
```

### 3.1 型

```cpp
struct InlineRun { std::u16string text; TextStyle style; };
struct InlineImage { std::shared_ptr<Image> image; Size size; };          // 行内画像（技術文書のアイコン等）
using InlineItem = std::variant<InlineRun, InlineImage>;
struct Paragraph { std::vector<InlineItem> items; std::vector<Annotation> annotations; ParagraphStyle style; };
// Annotation は richtext InlineAnnotation と同じ（Ruby{Group|Mono}, TateChuYoko, Emphasis, Warichu, Jidori）。範囲は段落全体の UTF-16 位置

struct LineShape { Pt length; Pt indentStart = 0; };                        // 行長と行頭の下げ
class LineShapeProvider { public: virtual LineShape at(int lineIndex, Pt blockOffset) const = 0; };
//   ConstantShape（普通の段落）/ RegionShape（排除領域を避ける）/ 将来: 吹き出しの形状走査

struct BreakLine { uint32_t itemStart, itemEnd; float ratio; Pt natural, width; int lineIndex; };
struct LineBox {
    std::vector<PlacedGlyph> glyphs;    // 論理座標（inline, block）、face、gid、xform、charIndex、fontSize
    Pt length; Pt extentBefore, extentAfter;   // block 軸の張り出し（ルビ・圏点で増える）
    size_t charStart, charEnd; bool hanging;
};
struct ParagraphFragment { std::vector<LineBox> lines; size_t charEnd; bool complete; };

class ParagraphLayouter {
public:
    // charStart から組み始め、maxLines（段の残り行数）まで。段またぎの続きは charEnd から再度呼ぶ
    ParagraphFragment layout(const Paragraph&, WritingMode, const LineShapeProvider&, size_t charStart, int maxLines);
};
```

### 3.2 richtext からの移植で変えるところ

| richtext | typeset | 変更点 |
|---|---|---|
| `VerticalShaper`（minikin itemize ＋ HarfBuzz 直叩き） | `Itemizer` ＋ glyphware `shapeRun` | フォールバックは `FontSet::resolve`。TTB は glyphware `Shaper::Direction` に追加。横倒しランの回転は物理化で付ける |
| `ShapedCluster { origin, advance, charClass, upright }` | 同じ＋`runIndex`（スタイル・face を引く） | サイズ混在（`InlineRun` ごとに style）に対応 |
| `LineItemBuilder`（縦の u/v 前提） | `ItemBuilder`（inline/block 論理座標） | 注記の付随グリフの座標を論理座標へ。横組みのルビは block 負側（上）。圏点も同様 |
| `LineBreaker::breakLines(items, lineLength, opts)` | `(items, LineShapeProvider&, opts)` | Greedy は現在行の `at(i)` を引くだけ。KP はノードに行番号を持たせ、行長は行番号で引く（`\parshape`） |
| `VerticalParagraphLayout` | `ParagraphLayouter`（途中から・行数上限つき） | 段またぎで**組み直さず続きから組む**形を基本にする（richtext は行長が変わる時に残り全部を組み直していた。同じ結果になるが、`charStart` から再開できる方が Region 側が単純） |
| `GlyphInfo { minikin::FontFakery }` | `PlacedGlyph { bool fakeBold, fakeItalic }` | minikin 型を消す |

### 3.3 行分割機会

- 和文: `CharClass` から直接（richtext と同じ。行頭禁則・行末禁則・分離禁止・ぶら下げ）
- 欧文: libunibreak `set_linebreaks_utf16` の結果を `Penalty` の有無に写す（単語内は Penalty(∞)、空白は Glue）
- 混在: 和欧の境界は常に候補（和欧間アキの Glue がそこにある）
- `letterSpacing` は richtext と同じくクラスタ間 Glue に足す

---

## 4. ブロックとページ（`block/`, `page/`）

### 4.1 ブロック

```cpp
struct Heading   { Paragraph para; int level; bool spanColumns = false; };     // 段抜き
struct ImageBlock{ std::shared_ptr<Image> image; Size size; enum { Inline, FloatStart, FloatEnd } placement; Paragraph caption; };
struct Rule      { Pt thickness; Color color; Pt lengthRatio = 1; };            // 罫線
struct Labeled   { Paragraph label; Pt labelWidth; std::vector<Block> body; bool ruleBetween; };   // 台本の名前欄＋本文、箇条書き、用語集
struct Spacer    { Pt size; };
struct Table;                                                                    // Phase 3（§5）
using Block = std::variant<Paragraph, Heading, ImageBlock, Rule, Labeled, Spacer, Table>;
struct Flow { std::vector<Block> blocks; };
```

`Labeled` は台本の中核。ラベル欄と本文で行送りを共有し、ラベルは本文 1 行目の位置に揃える（本文が段をまたいでもラベルは先頭側にだけ出る）。
本文の行長はラベル幅を引いたものになるので、これも `LineShapeProvider` で表現できる。

### 4.2 ページマスタとページ列

```cpp
struct PageMaster {
    Size size; struct { Pt top, bottom, inner, outer; } margin;   // 見開きの内外で指定
    WritingMode writingMode; int columns = 1; Pt columnGap = 0;
    std::optional<Paragraph> header, footer;      // 柱・ノンブル。テキストに {page} {title} {chapter} を埋め込む
    Pt headerOffset, footerOffset;
    bool duplex = false;                          // 左右ページで inner/outer を入れ替える
};
struct PageSequence { PageMaster first; PageMaster body; /* 後で: 左右別マスタ */ };
```

### 4.2.1 段組は第一級の概念にする（メモ 2026-09-08）

ノベル（2 段組の文庫・雑誌形式）も技術文書（2 段組論文形式、図表だけ段抜き）も段組が要る。
台本も「名前欄＋本文」の他に 2 段組で組むことがある。そこで

- 段数・段間は `PageMaster` に持たせるが、**Flow の途中で段数を変えられる**（`Section` ブロック:
  以降のマスタ／段数を切り替える。段の途中なら残りを埋めてから切り替える）
- 見出し・図・表は `spanColumns` で段抜き（全幅）にでき、その下で段を再開する
- 段のバランス取りは段抜きブロックの前で行う（実装済み: ページの再開点から段の長さを縮めて組み直す試行モード。
  `FlowLayouter` の `balanceColumnsBefore`）。最終ページで各段の行数を揃えるだけのバランス取りは同じ機構で足せる
- 縦組みの段は上下に並ぶ（richtext BlockLayout と同じ）。横組みの段は左右

### 4.3 Region と Flow の流し込み

```cpp
struct Region {                    // ページ内の 1 段
    Rect area; WritingMode wm;
    std::vector<Rect> exclusions;  // 回り込み用（画像の占有域）
    Pt used = 0;                   // block 軸で使った量
    LineShapeProvider shapeFor(Pt linePitch) const;   // 排除領域を避けた行ごとの行長
};

class FlowLayouter {
public:
    // Flow を PageSequence に流し、ページごとの DisplayList を返す
    std::vector<Page> layout(const Flow&, const PageSequence&, const FontSet&);
};
```

流し込みの規則（Phase 2 で入れるもの）:

- 段落は `ParagraphLayouter` に `maxLines` を渡して段に入るだけ組み、残りは次の段へ続きから
- `orphans` / `widows` を満たせない分割は行を次の段へ送る
- `keepWithNext`（見出し）は次のブロックの先頭 `orphans` 行まで一緒に入らなければ段を送る
- `breakBefore = Page` で改ページ。`spanColumns` の見出しは段の並びを分断して全幅に置く（その下で段を再開）
- 柱・ノンブルはページ確定後に `header` / `footer` の `{page}` 等を置換して組む（1 行）
- 回り込みは Phase 3: `ImageBlock::FloatStart` を Region の `exclusions` に登録し、その後の段落が `shapeFor` で行長を縮める

---

## 5. 表（Phase 3、TeX tabular 水準）

```cpp
struct ColumnSpec { enum { Fixed, Auto, Flex } kind; Pt width; float weight; Align align; };   // l/c/r/p{w}/X 相当
struct Cell { Flow content; int colspan = 1, rowspan = 1; Borders border; Pt padding; VAlign valign; };
struct Row  { std::vector<Cell> cells; bool isHeader = false; };
struct Table { std::vector<ColumnSpec> columns; std::vector<Row> rows; Borders outer; bool repeatHeader = true; Pt width = 0; };
```

- 列幅: Fixed はそのまま。Auto は各セルの min/max 内容幅（行長 0 と ∞ で組んで測る）から CSS 自動表と同じ配分。Flex は残りを weight で
- 罫線は `Borders`（上下左右の有無・太さ）。`\hline` `\cline` `\multicolumn` に対応する形
- 行はページ／段をまたいで分割できる（longtable）。ヘッダ行は繰り返す
- セル内は入れ子の Flow なので、段落・ルビ・画像はそのまま入る
- 縦組みの表（縦組み本文中の横組み表）は、表全体を横組みブロックとして置く（技術文書は横組みなので優先度低）

---

## 6. 入口と Python

```cpp
class Document {
public:
    FontSet& fonts();
    Flow& addFlow(PageSequence);
    void layout();                                   // → pages_
    const std::vector<Page>& pages() const;
    std::string toPdf() const;  std::string toSvg(int page) const;  Image render(int page, float dpi) const;
};
```

Python（`python/typeset_module.cpp`、pybind11）は `Document` / `Flow` / 各 Block / `Paragraph` / `TextStyle` / `ParagraphStyle` / `PageMaster` /
`Annotation` をそのまま出す。文字列は UTF-8 で受けて内部で UTF-16 へ。`render()` は `bytes`（ARGB）と幅高さを返し、NumPy には利用側で載せる。
低レベルとして `layout_paragraph(text, style, writing_mode, line_length) -> lines` も出す（ツールで行分割結果だけ欲しい場合）。

---

## 7. glyphware 側の変更 → 不要になった（Phase 1 で判断）

glyphware の `Face::hb()` は `hb_ft_font_create_referenced` で作られ、FT face のピクセルサイズに
スケールが縛られる。typeset はフォントユニットで位置を受け取りたいので、`font::FontSet::hbFont()` が
フォントのバイト列（`Face::data()`）から OT funcs の hb_font を自前で作り、TTB も features も
そこで直接指定する。glyphware は face の管理・メトリクス・アウトライン・マスク生成に使い、
シェイピングは typeset 側が HarfBuzz を直接叩く。richtext の `VerticalShaper` と同じ構図。

---

## 8. Phase 0 / 1 の作業項目

### Phase 0（骨格）

1. リポジトリ初期化、`ext/glyphware` submodule、CMake（presets / Makefile は richtext から流用）、vcpkg.json
2. `geom` / `writing_mode` / `dl` の型
3. backend 最小実装: Raster（矩形＋グリフマスク合成）、PDF（矩形＋ Identity-H グリフ、richtext `PdfWriter` / `SfntInfo` 移植）、SVG（矩形＋グリフパス）
4. `sample_dl`: 1 ページに矩形と「あA」を縦横の回転付きで置き、BMP / PDF / SVG に出す。3 つの座標が一致することを確認
5. `data/download_fonts.py` 流用、doctest の枝

### Phase 1（インラインコア）

1. `text/`: `CharClass` / `SpacingTable` / UAX#50 を richtext から移植。EAW テーブル生成スクリプト。libunibreak の包み
2. `font/FontSet`: Registry 包み、フォールバック解決、バリアブル軸インスタンス、縦メトリクス合成
3. glyphware に TTB / features を追加
4. `inl/`: Itemizer → Shaper → ItemBuilder（移植・論理座標化）→ LineBreaker（`LineShapeProvider`、Greedy 先行、KP は行番号対応）→ LinePlacer → ParagraphLayouter
5. `sample_inline`: 同じ文（約物・和欧混在・ルビ・縦中横・圏点・割注）を **縦組みと横組み**で組み、3 backend に出す。
   検算: 禁則違反 0、両端揃えの行長一致、richtext の `sample_vertical` と同じ文で同じ行分割になること

### Phase 2（ページ）

`Flow` / `PageMaster` / `Region` / `FlowLayouter`、`Labeled`、`Heading`（keepWithNext・段抜き）、柱・ノンブル、orphans/widows。
`sample_script`（台本: 名前欄＋本文、2 段、柱、ノンブル）と `sample_novel`（ラノベ本文: 縦組み 1 段、ルビ多め、章見出し、改ページ）。

---

## 9. 決めていないこと（実装しながら決める）

- Knuth–Plass の行番号依存化のコスト。Greedy で品質が足りるなら KP は PDF 出力時のみ
- 縦組み中の横組み表・図の扱い（技術文書は横組みなので後回し）
- マークアップ（`｜親《ルビ》`、`<ruby>` 等）の書式。台本ツール側の既存書式に合わせる可能性がある
- PDF のタグ付け（アクセシビリティ）は `Meta::StructTag` の余地だけ残す
