# 実装進捗

フェーズの内容と完了条件は `検討.md` 5 章、具体設計は `設計.md`。

## ビルド

```bash
# 事前準備: VCPKG_ROOT を設定。テスト用フォントは make fontdata（または richtext/data からコピー）
make fontdata

# 開発中は glyphware のローカルツリーを指す（未指定なら GitHub から FetchContent）
make prebuild GLYPHWARE_DIR=d:/work/kirikiri/glyphware
make build
make test

# サンプルはリポジトリルートで実行する（フォントを ./data/ から読む）
./build/x64-windows/Release/sample_dl.exe
```

## Phase 0: 骨格 — 完了

確認済み（`sample_dl` → `output_dl.png` / `.pdf` / `.svg`、2026-09-08）:

- ラスタ（216 dpi）、SVG（Inkscape で描き直し）、PDF（Inkscape + poppler で描き直し）の 3 つで
  **インクの外接矩形が 1px 単位で一致**（602×482 @ +59,+59）。RMSE は 1〜2%（ラスタライザの AA 差）
- 横組みベタ・縦組み（正立＋欧文横倒し −90°）・変形グループ（平行移動＋不透明度）・楕円の塗り／線・
  フェイクボールド・フェイク斜体・縁取りのみ・平体（scaleY 0.7）が 3 backend で同じ位置に出る
- doctest 11 ケース / 92 アサーション通過（行列の合成順・逆行列・回転の向き・平坦化・ラスタライザの面積・矩形塗り）
- CFF（Noto Serif CJK JP）は CIDFontType0、glyf（Noto Sans）は CIDFontType2 で埋め込まれ、ToUnicode が付く

内容:

- CMake / presets / Makefile / vcpkg.json（freetype, harfbuzz, zlib, stb, doctest）
- `cmake/glyphware.cmake` — `GLYPHWARE_DIR` ならローカルツリー、無ければ FetchContent
- `include/typeset/geom.hpp` — Pt / Point / Rect / Mat2 / Matrix / Color / Stroke / Path、平坦化
- `include/typeset/writing_mode.hpp` — WritingMode / TextOrientation / LogicalPoint / toPhysical
- `include/typeset/dl/display_list.hpp` — GlyphRun / PathItem / RectItem / ImageItem / Group
- `include/typeset/dl/glyph_transform.hpp` — グリフ固有の変形（3 backend 共有）
- `include/typeset/font/font_set.hpp` — face を開いてキーで引く（Phase 1 で Registry へ）
- `backend/raster` — glyphware のマスク＋自前合成、パスはスキャンライン AA、PNG 出力（zlib）
- `backend/pdf_writer` — richtext PdfWriter 移植（Identity-H、1 グリフ 1 Tm、ToUnicode、パス、クリップ、透明度）
- `backend/svg_writer` — グリフはアウトラインを defs + use、パス、クリップ
- `samples/sample_dl` — 横組み・縦組み（正立＋横倒し）・変形グループ・縁取り・平体を PNG / PDF / SVG へ
- `tests/` — doctest（幾何、変形、ラスタライザの面積）

## Phase 1: インライン組版コア — 完了

richtext の縦組みエンジンを書字方向非依存に一般化して移植した。座標は論理座標
（inline_ = 送り、block = 行の中心線からのずれ）で組み、物理化は `emitParagraph` の 1 箇所。

- `text/` — `CharClass`（JLReq 附属書 A）・`SpacingTable`（表 3）・`orientation`（UAX #50）を移植。
  `line_break` は libunibreak（UAX #14）の包みで、欧文単語内の分割可否だけに使う
- `font/FontSet` — face をキー／family 名で引き、`FontSpec` の family 列と文字カバレッジで
  フォールバックを解決する。シェイピング用 hb_font はフォントのバイト列から OT funcs で作り
  スケールを unitsPerEm にして**フォントユニットで**位置を受け取る（glyphware の `Face::hb()` は
  hb-ft でピクセルサイズに縛られるので使わない。**glyphware 側の変更は不要になった**）
- `inl/shaper` — Itemizer（スタイル・face・正立／横倒し）＋ HarfBuzz。正立は TTB（vert / vmtx /
  VORG が効く）、横組み・横倒しは LTR。ブロック軸は「em box の中心＝行の中心線」で、横組みの
  ベースラインは第一候補フォントの ascender/descender から `baselineOffset()` で決める
- `inl/item_builder` — Box / Glue / Penalty 列。注記（ルビ・縦中横・圏点・割注・字取り）の付く側は
  `annotationSide()`（縦: 右、横: 上）。圏点は字面（インク）の中心で揃える（U+FE45 が縦組み用字形のため）。
  和字同士の字間に `kanjiSkipStretch`（既定 0.125em）の伸びを入れ、追い出し（字間を空けて行末を揃える）を可能にした
- `inl/line_breaker` — Greedy / Knuth–Plass。行長は `LineShapeProvider::at(lineIndex)` で行ごとに取る
  （`\parshape`）。KP はノードに行番号を持つ。Greedy の伸び率は 1 を超えても揃える（上限 4）
- `inl/paragraph` — `Paragraph`（InlineRun 列＋注記＋ParagraphStyle）→ `ParagraphFragment`（LineBox 列）。
  改行で段落を分け、一字下げ、Start/End/Center 揃え、**`charStart` からの再開と `maxLines`**（段またぎ用）に対応
- `samples/sample_inline` — 同じ文（約物・和欧混在・ルビ（グループ／モノ）・縦中横・圏点・割注・字取り）を
  縦組みと横組みで組み、Knuth–Plass の例も出す

確認済み（2026-09-08、`output_inline.png` / `.pdf` / `.svg`）:

- 縦・横・KP の全段落で **禁則違反 0、両端揃えの外れ 0**（サンプルが検算して出力）
- ラスタ／SVG（Inkscape）／PDF（poppler）の外接矩形が 1〜2px 以内で一致、RMSE 5%（AA 差）
- doctest 18 ケース / 159 アサーション（文字クラス・UAX#14・シェイピングの送りと向き・
  約物のボディ圧縮と禁則ペナルティ・縦横の両端揃え・再開／行数上限・ルビの張り出し）

積み残し（Phase 1）:

- （済）熟語ルビ、行頭・行末でのルビの掛かり抑制、圏点の左側指定
- 縦組み中の割注は 2 行固定、横組みの割注は上下 2 行（位置の微調整は未検証）
- バリアブルフォントの軸インスタンス（wght 違いを別 face として持つ）は未対応
- 双方向テキスト（RTL）は未対応。SheenBidi は glyphware 内にあるので Itemizer に足せる
- 欧文のハイフネーションは無い（UAX #14 の分割機会のみ）

## Phase 2: ブロック／ページ — 完了（基本機能）

- `block/block.hpp` — `BlockStyle`（spaceBefore/After・orphans/widows・keepWithNext・breakBefore/After・spanColumns）、
  `ParagraphBlock` / `HeadingBlock` / `RuleBlock` / `SpacerBlock` / `LabeledBlock`（台本の名前欄＋本文）/ `SectionBlock`（段数変更）、`Flow`
- `page/page.hpp` — 判型定数（A4/A5/B5/B6/文庫/新書）、`Margins`（inner/outer）、`RunningText`（柱・ノンブル、`{page}` `{pages}` `{title}` 置換）、
  `PageMaster`（版面・段の矩形。縦組みの段は上下、横組みは左右。duplex で内外余白を入れ替え）、`Region`（段）
- `page/flow_layouter` — Flow をページ列へ。段落は段に入る行数だけ組み（`ParagraphLayouter` の `maxLines`）、残りは次の段へ
  `charStart` から続ける。orphans / widows、keepWithNext（次の段落が入らなければ見出しを巻き取って一緒に移す）、
  改段・改ページ、段抜き（内容のあるページでは改ページしてから全幅に置き、その下で段を再開）、ラベル付き段落
  （本文の行長を labelWidth + gap だけ縮め、ラベルは本文 1 行目と同じ位置）、罫線、柱・ノンブル（横組み、`{pages}` は 2 パス）
- `samples/sample_script` — A5 縦組みの台本（名前欄＋本文、場の見出し、柱・ノンブル、4 ページ）
- `samples/sample_novel` — B6 縦組み 2 段の小説（章見出しの段抜き＋改ページ、一字下げ、ルビ、ぶら下げ、Knuth–Plass、3 ページ）

確認済み（2026-09-08、`output_script_p*.png` / `output_novel_p*.png` と PDF / SVG）:

- 台本 4 ページ・小説 3 ページが流れ、グリフが版面内に収まる（テストで全グリフのペン位置を検査）
- keepWithNext の見出しが段末に残らず次ページへ移る（テスト）
- ラベル付き段落の本文が labelWidth + gap から始まり、折り返し行も揃う（テスト）
- 柱・ノンブルの置換と揃え（End / Center）

積み残し（Phase 2）:

- 段抜きは「段に内容があれば改ページ」。段の途中での段抜き（段のバランス取り）は未対応
- `SectionBlock` の段数変更は次のページから
- （済）keepWithNext は罫線・スペーサーを通る
- ラベルが本文より長い（複数行）ときは行数の大きい方を消費する（本文の全行が labelWidth ぶん縮むので衝突はしない）
- 左右ページでマスタを変えるのは未対応。柱・ノンブルの左右反転は `RunningText::mirrorOnEven` で可

## Phase 3: 画像・罫線・回り込み・表 — 完了（基本機能）

- `image/` — stb_image による読み込み（PNG / JPEG / BMP / GIF）、PNG エンコード（zlib、ラスタ backend と共用）、base64。
  JPEG は元バイト列を保持して PDF へ素通し（DCTDecode）。それ以外は RGB を Flate、透明度は SMask
- backend — PDF: 画像 XObject（`cm` で単位正方形→ページ）、SVG: `<image href="data:...;base64,...">`。ラスタは Phase 0 から対応済み
- `block::ImageBlock` — Block（流れに置く、Start/Center/End）/ FloatStart / FloatEnd（回り込み）。サイズは pt で指定
  （片方 0 なら縦横比維持、段より大きければ縮小）。キャプション付き
- 回り込み — `Region::exclusions`（排除領域）と `RegionLineShape`（行 i の帯にかかる排除領域を避けた最大区間を
  `LineShape` として返す）。段落は常にこれ経由で組むので、ラベル付き段落（`baseIndent`）とも合成される
- `block::TableBlock` — 列幅（固定／自動: 自然幅から比例配分、割れない最長の語を下回らない）、罫線（外枠・内側・ヘッダ下、
  縦横の有無）、colspan、ヘッダ行のページまたぎ繰り返し、セル内揃え、fullWidth / align。列は inline 軸、行は block 軸なので
  縦組みでも同じコードで組める（縦組みの表は列が上下・行が右→左）
- `RuleBlock` を矩形塗りに変更（線幅が backend で揃う）
- 行分割: 揃えない段落（ragged）では縮みを使わず自然幅で収まりを判定（TeX の ragged-right と同じ）。
  揃えないのに縮んだ幅で判定するとセル内で行末がはみ出していた
- 文字クラス: ASCII の約物（. , : ; ! ? ( ) [ ] { }）を欧文用文字に変更。"1." "e.g." "f(x)" に JLReq のアキが入らなくなった。
  欧文に挟まれたハイフン類（en dash 等）も欧文として組む（"Knuth–Plass"）
- `SectionBlock`（段数変更）は内容のあるページでは改ページしてから効く
- `samples/sample_tech` — A4 横組みの技術文書（見出し、回り込みの図＋キャプション、中央の図、自動列幅の表（ヘッダ繰り返し・colspan）、
  罫線、途中から 2 段組＋右寄せ回り込み）3 ページ

確認済み（2026-09-08、`output_tech_p*.png` / `.pdf` / `.svg`）:

- 回り込み: 図の横で行長が縮み、図の下で全幅に戻る（テスト: 横で x ≥ 図の右端、下で左端から）
- 表: 14 行がページをまたぎ、各ページにヘッダが 1 回ずつ出て罫線が版面内に収まる（テスト）
- ラスタ／SVG（Inkscape）／PDF（poppler）で画像込みのページの描画が一致（RMSE 数%）
- doctest 26 ケース通過

積み残し（Phase 3）:

- （済）行内画像。回り込みの図は残りに入らなければ図ごと次の段へ移す（排除領域の持ち越しは不要）
- 表: （済）セル内の縦位置、行の途中でのページまたぎ、縦組みの表の検証、rowspan を含む塊の分割
- 図番号・表番号の自動採番、相互参照、目次
- 吹き出し（形状 → 行長関数）は `RegionLineShape` と同じ口で足せる。ユーザー判断で将来へ

## 外部オブジェクトの差し込み（数式・グラフ） — 完了（2026-09-08）

本体は数式や図の知識を持たず、「ハンドラ名＋ソース文字列＋パラメータ」を書いておくと組版時に登録済みの
ハンドラを呼び、返ってきた箱（大きさ・ベースライン）で配置して描画命令を出力へ流す。`obj/` に集約。

- **`obj::ObjectRegistry`**: `add(name, fn)` で関数ハンドラ（`ObjectRequest` → `ObjectResult{size, baseline, items, fonts}`。
  GlyphRun で返せば PDF でも字として埋め込まれる）、`addCommand(name, cmd)` で外部コマンド
  （要求を JSON ファイルに書いて `cmd <request.json>` を実行、標準出力の SVG を読む）。
  結果は (ハンドラ, ソース, パラメータ, サイズ, 行内か, 縦か) でキャッシュ（多パスで同じ式を何度も組むため）。
  未登録・失敗は `errors()` に残し、本文には `[handler: error]` の代替テキストを置いて続行する
- **`obj/svg_import`**: SVG サブセット → 表示リスト。svg(width/height/viewBox、単位 px/pt/mm/cm/in/em/ex)、
  g(transform・塗り・線の継承)、path（全コマンド、弧は 3 次ベジェへ）、rect(rx)・circle・ellipse・line・
  polyline・polygon、defs + use、fill/stroke/stroke-width/opacity/fill-rule（属性と style=）。text・画像・
  クリップ・グラデーションは無視。ベースラインは `<!-- typeset baseline="12.3" -->`（pt）か、MathJax の
  `vertical-align: -0.5ex` から
- **行内**（`Paragraph::addObject(handler, source, params, style)`）: U+FFFC 1 文字の箱。横組みは本文の
  ベースラインにオブジェクトのベースラインを揃える（無ければ中心）。縦組みは横倒し（時計回り 90°）で段の中心へ。
  **行送りの箱 [-pitch/2, +pitch/2] から出るぶんは、その行の前後に追加の送り**（`LineBox::extraBefore / extraAfter`、
  TeX の lineskip 相当）。行内画像も同じ。ルビや横倒しの張り出しは従来どおり行間に収める（広げない）。
  行位置は `ParagraphFragment::lineCenterOffset(li)`、消費量は `blockExtent()` で、流し込み側は行数上限で組んだ後に
  段に入らなければ行数を減らして組み直す（`layoutFitting`）
- **別行立て**（`ObjectBlock` / `Flow::addObject`）: 行方向に揃え（既定は中央）、段より大きければ縮小。
  `numbered` で式番号（`FlowLayoutOptions::equationFormat`、既定 "({n})"）を行末に、行送り方向の中央で置く。
  `label` を付ければ `{ref:label}` が「(1)」になる。`caption` の `{eq}` も同じ
- Python: `ObjectRegistry.add(name, fn)`（fn は request dict を受けて SVG 文字列か `{"svg", "baseline"}` を返す）、
  `add_command`、`Paragraph.add_object`、`ObjectBlock`、`Flow.add_object`、`layout(objects=..., equation_format=...)`
- サンプル: `samples/sample_objects.cpp`（分数を GlyphRun で返す関数ハンドラ、外部コマンド
  `samples/handlers/plot.py`（純 Python の SVG 生成）、SVG 文字列の直接読み込み、未登録の代替テキスト）、
  `python/examples/objects.py`（Python 関数ハンドラの棒グラフ）。PDF（poppler 描画）とラスタの一致を確認
- テスト 6 件（SVG 読み込みの要素・変形・単位・ベースライン、レジストリのキャッシュ／未登録、
  行内のベースライン一致・別行立ての中央揃え／縮小／式番号／参照、縦組みの横倒し）

- 行送りが広がった行の後ろでも回り込みが正しく効くように、`LineShapeProvider::at(lineIndex, extraBlock)` を足し、
  段落レイアウタは行の追加送りの累積を `OffsetLineShape` で渡して組み直す（不動点まで最大 3 回。追加が無ければ 1 回）

積み残し: SVG の `<text>`（フォント解決が要る）

### MicroTeX ハンドラ（`handlers/microtex/`、任意ターゲット） — 完了（2026-09-08）

LaTeX 数式のサンプルハンドラ。本体には依存を足さず、`-DTYPESET_HANDLER_MICROTEX=ON -DVCPKG_MANIFEST_FEATURES=microtex`
で有効にする（tinyxml2 は vcpkg の feature `microtex`）。MicroTeX（MIT、コミット固定で FetchContent）の
CMakeLists は使わず、`src/` から platform / samples を除いて静的ライブラリ `microtex_core` にする。

- `tex::Font` → glyphware の Face（MicroTeX 付属フォント res/fonts は `Font::create(file)` でファイルから、
  `\text{}` 用の "Serif"/"SansSerif" は `MicroTexOptions::textFamily / sansFamily` の FontSet キーへ）
- `tex::TextLayout` → typeset のシェイパで幅・高さ、`tex::Graphics2D` → 変形行列を持ち、`drawChar` は GlyphRun、
  矩形・線・丸角は PathItem。`scale(sx, sy)` はグリフの `xform` に畳む（伸縮する括弧）
- TeXRender の幅・高さは int なので 10 倍で組んで 1/10 に縮める。ベースラインは `getBaseline()` の比から
- 結果: 数式のグリフが cmr10 / cmmi10 / cmsy10 / cmex10 等の**フォントとして PDF に埋め込まれる**（サブセット化・検索可）。
  `sample_microtex`（A4、行内の分数・総和、別行立ての積分・行列・場合分け・Maxwell、`\text{}` の和文、
  未定義コマンドの代替テキスト）で確認。poppler 描画とラスタの RMSE 3%
- params: `style=display|text`、`color=#rrggbb`

制限: MicroTeX は OpenType MATH ではなく付属フォント（OFL / Knuth ライセンス）で組む。res/ の場所はビルド時に埋め込む
（`MicroTexOptions::resDir` で差し替え可）。`Font::create` が static なので FontSet はプロセスで 1 つ

## 細かい積み残しの対応（2026-09-08）

- **ルビの行頭・行末での掛かり抑制**（JLReq 3.3.6）: 行を組んだ後、Box に付いた注記グリフ（ルビ・圏点）が行頭より前／
  行末より後ろへ出ていれば、そのぶん内側へずらす（`paragraph_layouter` の行組み立て）。行の途中の掛かりは隣の字の上なので
  触らない。Greedy / KP どちらでも効く。縦横でテスト
- **圏点の反対側指定**: `Annotation::emphasis(…, oppositeSide=true)` で注記側の反対（縦: 左、横: 下）に付ける。Python も
- **見開きで柱・ノンブルを鏡像に**: `RunningText::mirrorOnEven`（duplex の偶数ページで Start/End を入れ替える）
- **keepWithNext が罫線・スペーサーを通る**: 見出し＋罫線＋段落を一緒に巻き取る。罫線・スペーサーは保留（Pending）を
  リセットせず、巻き戻しでは保留ブロックから直前のブロックまでを置き直す。テスト（keepTogether の段落が入らず次ページへ
  移るとき、見出しと赤い罫線も一緒に移る）
- **縦組みの表の検証**: テスト追加（ヘッダ行が右端、行が左へ進む、罫線・グリフが版面内）
- **索引**: 本文の `{index:用語}` / `{index:よみ|用語}` を `resolved()` で取り除きながら（後ろの注記の位置も詰める）
  いまのページ番号で集め（`RefInfo::index`。読みはカタカナ→ひらがな・長音符除去で正規化）、`IndexBlock` が次のパスで
  読み順に用語とページ（"1, 3"）を点線付きで並べる。見出し文字（あ・か・さ… / A–Z / 0–9 / 他）を行ごとに挟む。
  目次と同じ多パス（IndexBlock があれば 3 パスまで）。Python `IndexBlock` / `Flow.add_index`、Markdown は `[index]`。
  制限: ページ番号は段落を解決した時点のページ（段またぎで後半に移った用語は前のページになる）
- **表: rowspan を含む行の塊の分割と、分割時の罫**（`placeSplitGroup`）: 段より高い塊（rowspan でつながった行、または 1 行）を
  行単位で入るところまで置き、1 行が入らなければ行の途中で分ける。行をまたぐセルの内容はカーソル（段落番号・文字位置）で
  続きから流す。段が変わるときは下罫を引き、次の段でヘッダ行を繰り返して**ヘッダ罫を引いてから**続ける（従来はヘッダと
  続きの間の罫が抜けていた）。行の途中で分けることになるのに残りが段の 30% 未満なら次の段から始める（段末の空セグメントを
  作らない）。同じ段に次の行が入るときは段を替えずに続ける。テスト（rowspan 3 行の塊が 2 ページにまたがり、内容が失われない、
  ヘッダが各ページ 1 回）
- **タブの展開**: `preserveSpaces` の段落では、組む前にタブをタブ位置（`ParagraphStyle::tabWidth`、既定 4 桁）までの空白に
  展開する（シェイパはタブを幅 0 のグリフにするので消えていた）。注記の位置もずらす。Markdown のコードブロックで確認
- **Markdown のコードブロックの色付け**: fence の言語名があり Pygments（任意依存 `typeset[highlight]`）が入っていれば、
  トークンごとに色・太字・斜体の run にする（`highlight` / `highlight_style` / `tab_width` オプション）。無ければ単色
- **脚注のページごとの番号**（`FlowLayoutOptions::footnotePerPage`、Python `footnote_per_page`、Markdown `footnote-per-page`）:
  `newPage()` でカウンタを 0 に戻す。段落がページをまたぐときは、次のページに載る断片を組む前に `renumberFootnotes` で
  その位置以降の記号と注の頭を新しいページの番号に振り直す（記号の長さが変わるぶん注記の位置もずらす）。テスト
  （1 ページ目 1,2,3 → 2 ページ目 1。またいだ段落の後半の記号も 1）
- 積み残しのうち「回り込みの排除領域の次の段への持ち越し」は、回り込みの図が残りに入らないときは図ごと次の段へ移すので
  実質不要。「ラベルが複数行のときの衝突」も本文の全行が labelWidth ぶん縮むので起きない（記述を整理）

## 公開（2026-09-08）

- GitHub `wamsoft/jtypeset`（public、MIT）に push。ドキュメントサイト https://wamsoft.github.io/jtypeset/ を GitHub Pages で配信
  （MkDocs + Doxygen + pdoc + サンプル PDF）
- PyPI に `jtypeset 0.1.0` を公開（Trusted Publishing。Windows / manylinux_2_28 / macOS arm64・x86_64 × CPython 3.9〜3.13 と sdist）。
  素の venv で `pip install "jtypeset[md]"` → `jtypeset-md` で MicroTeX 数式込みの PDF ができることを確認
- CI で直したこと: vcpkg の `builtin-baseline`（公開リポジトリに存在するコミットに。manylinux 内では浅く fetch）、
  Windows の compiler 検出（preset に CXX=cl、msvc-dev-cmd）、GCC での include 漏れ（`scripts/gcc_syntax_check.sh`）、
  macOS x86_64 は Apple Silicon でクロスビルド、artifact アップロード失敗の再試行

## MicroTeX を既定に（2026-09-08）

- `TYPESET_HANDLER_MICROTEX` を既定 ON、tinyxml2 を vcpkg の基本依存に（feature は廃止）。MicroTeX は FetchContent（コミット固定）
- Python 拡張 `_jtypeset` に `typeset_microtex` をリンクし、`ObjectRegistry.add_microtex(name, fonts, text_family, sans_family, res_dir)`
  と `HAS_MICROTEX`、`jtypeset.microtex_res_dir()` を追加。数式フォント（res、2MB・OFL / Knuth ライセンス）は
  `jtypeset/microtex_res/` としてパッケージに同梱（ビルドツリーにもコピー、wheel にも入る）
- `jtypeset.md` の `math.handler` は既定 `auto`（MicroTeX があれば使う）。`microtex` / `mathtext` / `command` / `none`
- サンプル Markdown と docs/samples の PDF を MicroTeX 数式込みで再生成（行列・場合分けを追加）

## 公開の準備（2026-09-08）

- README を一般ユーザー向けに書き直し（概要 → Python 版の入手と使い方 → ビルド → 技術解説 → ライセンス）。元の要件メモは 検討.md へ
- ドキュメントサイト: `docs/`（index / markdown（Markdown → PDF の手引き）/ python_guide / cpp_guide / architecture / build）を
  MkDocs（readthedocs テーマ）で組み、Doxygen の HTML を `cpp/`、pdoc の HTML を `python/` に同梱して `build/site` に出す
  （`make site`）。`.github/workflows/docs.yml` が GitHub Pages に配信する（Settings → Pages → Source を GitHub Actions に）
- GitHub リポジトリは `wamsoft/jtypeset`（public、MIT）。`LICENSE` を追加、pyproject に license / urls
- CI: `ci.yml`（Windows / Linux で vcpkg + preset でビルド・テスト・サンプル実行）、`wheels.yml`（cibuildwheel で
  Windows / manylinux / macOS × CPython 3.9〜3.13 の wheel と sdist、タグ `v*` で PyPI へ Trusted Publishing）。
  いずれも手元では実行できていないので、最初の push で直す前提

## リファレンスの生成（2026-09-08）

- **C++**: `docs/Doxyfile`（入力は `include/typeset` と `docs/cpp_guide.md`。日本語、EXTRACT_ALL、ソースブラウザ、dot 無し）。
  `make docs`（`DOXYGEN=path` で実行ファイル指定）または CMake の `docs` ターゲット（Doxygen が見つかれば）→
  `build/docs/cpp/html/index.html`。`docs/cpp_guide.md` が使い方・レイヤー・座標の約束・拡張点のトップページ。
  ヘッダのコメントは Doxygen 形式（`///` と `/** */`）で、生成時の警告は 0
- **Python**: pybind の全クラスに docstring を付け、`m.doc()` に流れを書いた。`make pydocs` で
  pybind11-stubgen が型スタブ `python/jtypeset/_jtypeset/__init__.pyi`（IDE 補完・型検査。wheel にも入る）を、
  pdoc が `build/docs/python/index.html` を生成する。`typeset/__init__.py` は `__all__` を持ち、再エクスポートが pdoc に載る。
  手書きの `docs/python_guide.md` に最小の例・型の対応表・表／画像／目次／索引／脚注・外部オブジェクト・
  `jtypeset.md` の front matter 一覧
- 手元の Doxygen はポータブル版を一時的に使って確認した（システムには入れていない）

## 配布と Markdown → PDF — 完了（2026-09-08）

配布まわりの確認:

- **FetchContent**（`GLYPHWARE_DIR` 未指定）: `cmake/glyphware.cmake` の GIT_TAG が `main` だったが glyphware は `master`
  なので失敗していた → 直して、別ビルドディレクトリでフルビルド＋テスト（42 ケース）通過を確認
- **pip wheel**（scikit-build-core）: `$env{VCPKG_ROOT}` は cmake.args で展開されないので、CMakeLists 側で toolchain が
  無ければ `VCPKG_ROOT` から補うようにした。Windows は `x64-windows-static-md`（静的ライブラリ＋動的 CRT。
  `x64-windows-static` の /MT は Python の /MD と衝突する）。`dist/jtypeset-0.1.0-cp312-cp312-win_amd64.whl`（1.9MB）を
  別 venv に入れて import・変換できることを確認
- Python パッケージを `typeset/`（`__init__.py` が拡張 `_jtypeset` を再エクスポート）＋ `typeset/md` に変えた。
  ビルドツリーでは `<build>/python/<Config>/typeset/` に拡張と .py をまとめる（`typeset_py_sources` が毎回コピー）

`jtypeset.md`（Markdown → Flow → PDF、`pip install jtypeset[md]`、CLI `jtypeset-md`）:

- markdown-it-py（footnote / front_matter / dollarmath プラグイン）のトークン列を順に Flow へ写す。要素とブロックはほぼ 1:1
- front matter（`Options`）: 題名・著者・判型・書字方向・段数・余白・フォント・サイズ・行送り・一字下げ・目次・採番・
  柱／ノンブル・リンクの扱い・数式ハンドラ・図表式番号の書式。CLI の指定は front matter より優先
- 数式: `math.handler = command`（外部コマンド → SVG）か `mathtext`（matplotlib。任意依存。venv で確認）
- フォント未指定時は `data/` の Noto → OS のフォントを探す。**TTC** は face 番号でディレクトリを読み（`parseSfnt(…, faceIndex)`）
  サブセット化して埋め込む（丸ごとは埋め込まない）。**fsType**（OS/2）を見て Restricted License とビットマップのみは
  埋め込まず警告、サブセット不可は丸ごと埋め込む。游明朝・游ゴシック（fsType 0x8 Editable）で確認
- pybind の `std::vector` 属性はコピーを返すので、`items.append` ではなく Python のリストを作って代入する（変換器内の注意点）
- 脚注の記号が番号に置き換わるとき、後ろのルビ等の注記の位置をずらす（`numberFootnotes`）
- `samples/markdown/report.md`: 横組み A4 3 ページ、`--vertical --paper A5` で縦組み 7 ページ

制限: 入れ子の箇条書きは記号を全角空白で押して字下げしている（ListBlock に字下げは無い）。表のセル内は 1 段落。
定義リスト・タスクリスト・HTML の直書きは未対応

## 脚注 — 完了（2026-09-08）

- `Paragraph::addFootnote(note, markerStyle)`: 本文のその位置に記号の run（text は `{fn}`）が入り、注の段落を持つ。
  記号は `superscriptStyle(body)`（0.6 倍・注記側へ 0.6em）で上付きにする。`TextStyle::baselineShift`（em、注記側が正）を
  新設し、シェイパで block をずらす（横組み: 上、縦組み: 右）
- FlowLayouter は段落を組む前に `numberFootnotes` で記号を番号（`footnoteMarkerFormat`、文書を通して連番）に置き換え、
  注の頭に `footnoteLabelFormat`（"{n} "）を付ける。続きから組むとき（charStart0 > 0）は、それより前の脚注は
  前の段で数え済みとして番号を合わせる
- 断片に載る脚注の高さ（初回は罫のぶんも）を段末に確保（`Region::reserved`。`remaining()` から引く）し、入らなければ
  行数を減らす／次の段へ送る。注は `Region::footnotes` に溜め、`finishPage()` で段末に罫（行長の 30%）＋注を描く
- keepWithNext の巻き戻しでは確保量と注も戻す（`Pending::reservedBefore / footnotesBefore`）。組み直し（試行）は
  再開点の採番状態に `footnoteCount` を含めた
- Python: `TextStyle.baseline_shift`、`superscript_style()`、`Paragraph.add_footnote(note, marker_style)`、
  `layout(footnote_marker_format=, footnote_label_format=)`
- `sample_report` の「はじめに」に 2 つ。テスト 2 件（記号が上付き・注が版面の下端側で本文と重ならない・番号の連番、
  注が入らないときの次ページ送り）

制限: 表のセル・キャプション内の脚注は番号にならない（記号が `{fn}` のまま残る）。ページごとの番号は `footnotePerPage`

## レポート向け機能 — 完了（2026-09-08）

積み残しの残り（行内画像・セル内の縦位置・行の途中でのページまたぎ・最終ページの段揃え）と、
レポート作成に必要な機能をまとめて入れた。`samples/sample_report`（A4 横組み 5 ページ）で確認。

- **見出しの自動採番**（`HeadingBlock::numbered`。レベル 1 は "1."、以降 "1.1"）、**PDF のしおり**
  （`dl::Bookmark` → PdfWriter が /Outlines を組む。レベルで木にする）
- **相互参照**: `BlockStyle::label` を付けたブロックの番号／ページを、本文の `{ref:label}` `{page:label}` で参照。
  **図表番号**: `ImageBlock::caption` の `{fig}`、`TableBlock::caption`（表の上）の `{table}`（書式は `FlowLayoutOptions`）。
  **目次**（`TocBlock`: 見出し・点線・ページ番号、レベルで字下げ）。これらは 2〜3 パスで解く
  （前パスの番号・ページ・見出し一覧を次パスの置換に使い、安定したら止める）
- **箇条書き**（`ListBlock`: 記号／番号、ラベル付き段落の機構）、**コードブロック**
  （`ParagraphStyle::preserveSpaces` で空白を固定幅の箱に、`BlockStyle::background` + `padding` で背景、`keepTogether`）
- **行内画像**（`Paragraph::addImage`。U+FFFC 1 文字ぶんの箱として組版され、中心が行の中心線）
- 表: `TableCell::valign`（上・中央・下）、**段より高い行を段の境で分ける**（セルごとに続きから組み、ヘッダ行を繰り返す）、
  キャプション＋ヘッダ＋最初の本文行が入らなければ先に段を進める（ヘッダだけが段末に残らない）
- **最終ページの段揃え**（`FlowLayoutOptions::balanceLastPage`。段抜きと同じ再開点からの組み直し）
- keepWithNext の巻き取りで見出しだけのページが空のまま残る問題、段数変更（`SectionBlock`）の直前の見出しが取り残される問題、
  組み直しで見出し番号が進む問題（再開点の採番状態を保存して戻す）を修正

積み残し: 脚注、索引、表の行の途中で分けたときの罫の見え方の調整、rowspan 行の分割、縦組みの表の検証

## 積み残し上位 3 件 — 完了（2026-09-08）

- **段の途中での段抜き（段のバランス取り）** — `Flower` がページごとに「再開点」（先頭ブロック・段落番号・
  charStart・ラベル配置済みか）と段の初期矩形を持ち、段組ページの途中に `spanColumns` ブロックが来たら、
  そのページの段の内容を再開点から**段の長さを縮めて組み直す**（試行モード: あふれたら新しいページを作らず
  overflow を立てて中断）。長さを少しずつ増やして収まったところで、全幅の帯に段抜きブロックを置き、その下で段を再開する。
  行分割は段の幅だけで決まるので組み直しても行の切れ目は変わらない。段抜きが 2 回来たら 2 回目は前の段抜きの後から
  再開点を取る。試行中に改ページ・段数変更が来たら（前の内容がある限り）あふれ扱い
- **表の rowspan** — セルをグリッド（owner[row][col]）に割り付け、行の高さは rowspan 1 のセルで決めて、
  またぐセルの高さが足りない分は最後の行へ足す。横罫は上下のセルが違うところだけ（またぐセルの内部は引かない）、
  縦罫は左右のセルが違う境だけ。rowspan でつながった行はまとめて置く（ページの境で分かれない）。ヘッダ行は先頭の連続分を繰り返す
- **熟語ルビ**（`RubyMode::Jukugo`、`|` 区切り） — 各部分を組んで、全部が親 1 字に収まればモノルビと同じ、
  熟語全体には収まるなら中付きの希望位置から前後の重なりを解消して並べる（隣の親字へ掛かる、親は広げない）、
  熟語全体でも収まらなければグループルビとして親字間を広げる（JLReq 3.3.8 の 3 段階）

確認済み: `sample_tech` 3 ページ目（段の途中の段抜き見出し、rowspan の表は 2 ページ目）、`sample_novel`（「にんげんじゅう」が
人間中の 3 字にまたがる）。doctest 29 ケース / 565 アサーション（段抜き前の左右段の最下行の差が 1 行以内、
rowspan の横罫が右列だけ、熟語ルビが親を広げない／広げる判定）

## Phase 5: Python バインディング — 完了（基本 API）

- `python/typeset_module.cpp`（pybind11）、`python/CMakeLists.txt`（psdparse 準拠: pip の pybind11 を `-m pybind11 --cmakedir` で探す）、
  `pyproject.toml`（scikit-build-core）。CMake オプション `TYPESET_BUILD_PYTHON=ON` で `build/.../python/Release/jtypeset.cp312-win_amd64.pyd`
- 公開: `FontSet` / `TextStyle` / `ParagraphStyle` / `Paragraph`（`annotate`）/ `Annotation`（ruby / tate_chu_yoko / emphasis / warichu / jidori）/
  `Flow`（add_paragraph / add_heading / add_labeled / add_rule / add_image / add_table / add_section / 改ページ）/
  `ImageBlock` / `TableBlock` 一式 / `PageMaster` / `PageSequence` / `RunningText` / `paper.*` / `FlowLayouter.layout` →
  `Page`（`render` → ARGB bytes、`save_png`、`save_svg`、`to_svg`）/ `save_pdf` / `load_image` / `image_from_rgba`
- 低レベル: `layout_paragraph(fonts, para, writing_mode, line_lengths=[...], default_length)` — 行ごとの行長を渡して
  行の文字範囲と長さを返す（`\parshape`。回り込み・吹き出しツールの土台）
- `python/examples/script.py` — 台本 PDF を Python から組む例（3 ページ、PDF / PNG / SVG）

確認済み: 例が動き、`pages: 3` / PDF 269 KB。行長 [80, 120] + 既定 160 の指定が行ごとに効く

積み残し: pip wheel（scikit-build-core）は未検証。`FontSet.load_file` は UTF-8 パスを ifstream に渡すので
Windows の非 ASCII パスは `load_bytes` を使う。NumPy 連携（`render` は bytes を返す）

## Phase 6: PDF 品質 — 完了（サブセット化・圧縮）

- **hb-subset** で使用グリフだけを残す（`HB_SUBSET_FLAGS_RETAIN_GIDS` でグリフ ID を保持するので Identity-H の CID は不変。
  GSUB/GPOS/GDEF/vmtx 等は落とす）。フォント名に 6 文字の接頭辞（`ABCDEF+Name`）。失敗時は full embed に戻して警告
- コンテンツストリーム・ToUnicode・フォントファイルを **Flate** 圧縮（`setCompressStreams(false)` で無圧縮に）
- 結果: 台本 4 ページ 29 MB → 274 KB、技術文書 3 ページ（画像 3 枚込み）→ 457 KB。poppler での描画はラスタと一致（RMSE 4〜6%）

積み残し: `TJ` でのグリフまとめ（1 グリフ 1 `Tm` のまま。Flate 後のサイズには効かない）、しおり・リンク・ページラベル、
PDF/A・タグ付け、バリアブルフォントのインスタンス化（wdth 等は水平スケールで書く）

積み残し（Phase 0 内）:

- ラスタのストローク結合は丸で近似（Miter / Bevel は未実装）
- PDF / SVG の ImageItem は未対応（Phase 3 で画像と一緒に）
- PDF は full embed・無圧縮（Phase 6）
