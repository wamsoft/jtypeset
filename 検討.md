# 元の要件メモ（README から移動）

台本、小説、絵入り技術文章、漫画用の吹き出し処理、などを想定した
縦書き対応した、シンプルな日本語組版ライブラリを C++ で構築したい

- もっぱらツール作成に利用
- ゲームエンジンなどにも組み込めるようなもの。ベクターデータでの処理、SVG 出力、PDF 出力も視野にいれる
- いままで作っているライブラリ資産で活用できるものは活用（richtext: パラグラフレベル、横書きは minikin、縦書きは独自処理。
  glyphware: FreeType + HarfBuzz + ICU の一部をキャッシュ込みでまとめたもの）
- 必要になってくるもの: ルビ処理、日本語組版全般、罫線処理、ページ処理、画像差し込み、まわりこみなど
- C++ のライブラリ + Python でのバインドができるとよい

---

# typeset 検討メモ（第 1 回）

README の想定（台本・小説・絵入り技術文書・漫画吹き出し／縦書き対応／ツール利用が主・
エンジン組み込みも／ベクター・SVG・PDF／Python バインド）に対して、
既存資産（`richtext` / `glyphware`）の棚卸しと、新ライブラリの骨格・判断点・進め方を整理する。

---

## 1. 既存資産の棚卸し

### 1.1 richtext（d:/test/richtext、約 12.4k 行）

ゲームエンジン向けの**リアルタイム描画**ライブラリ。横組みは minikin（ICU + HarfBuzz 内包）、
縦組みは自前。直近の縦組み Phase 1〜5 で、組版エンジンとして流用価値の高い部品が揃っている。

| 部品 | 内容 | typeset での扱い |
|---|---|---|
| `CharClass` / `SpacingTable` | JLReq 附属書 A の文字クラス、表 3 のアキ量（自然値・伸び・縮み） | **そのまま移植**。書字方向に依存しない |
| `LineItem` / `LineBreaker` | Box / Glue / Penalty、Greedy と Knuth–Plass、禁則＝Penalty(∞)、ぶら下げ＝負幅 Penalty | **移植＋拡張**（行ごとに行長が違う `\parshape` 相当が要る。後述） |
| `LineItemBuilder` | クラスタ列＋注記 → アイテム列。約物の半角詰め、和欧間、禁則、ルビ・縦中横・圏点・割注・字取り | **移植＋一般化**。今は縦専用（u/v 座標・`VerticalShaper::Result` 前提） |
| `InlineAnnotation` | ルビ（グループ／モノ）・縦中横・圏点・割注・字取りの指定 | そのまま移植（熟語ルビ、行頭行末での掛かり抑制は積み残し） |
| `VerticalShaper` | minikin `itemize()` でフォールバック解決 → HarfBuzz を TTB 直叩き。横倒しランは LTR で組んで回転 | **置き換え**。フォールバック解決は glyphware の itemization へ、シェイピングは glyphware `Shaper` へ（TTB 追加が必要） |
| `BlockLayout` | 連結コンテナ・段・ページ分割、行長が変わる所で組み直す（TLF 流） | 考え方は継承。ページ／段／版面のモデルは作り直す（縦専用、段抜き・見出し・柱なし） |
| `GlyphTransform` | フェイク幅／斜体／スケール／回転を 1 行列に畳む。描画と PDF で共有 | そのまま移植 |
| `PdfWriter` / `SfntInfo` | Identity-H、CIDFontType0/2 full embed、1 グリフ 1 `Tm`、ToUnicode、`Tr` で装飾 | **移植＋強化**（サブセット化・Flate・画像・パス・しおり） |
| `Raster` / `GlyphRenderer` | 8bit マスク合成、変形をアウトラインに焼き込み、2x2＋1/4px 位相でキャッシュ | 移植（ラスタ backend）。テクスチャアトラスは対象外 |
| `TagParser` | HTML 風インラインタグ → スタイル区間。タイミング系タグ（ゲーム用）を含む | 組版に関係する部分だけ参考にする。縦組み非対応 |
| 横組み経路（`TextLayout` 等） | minikin 依存。**JLReq のアキ量処理は無い**（約物の詰め・和欧間は縦のみ） | 使わない |

richtext の設計文書で確定している方針のうち、そのまま引き継ぐもの:

- 「1 文字＝1em の箱」ではなく **Box / Glue / Penalty 列を解く**（追い込み・追い出し・両端揃えが調整比 1 つに統一される）
- 組版結果を**配置済みグリフ列で切り**、その下に描画 backend と PDF backend を並べる（再シェイピングさせない）
- 注記（ルビ等）は描画時の後処理ではなく**組版アイテムに落とす**（行長に効くため）
- `Tm` にフォントサイズを入れない、y-up/y-down の共役、等の嵌まり所

### 1.2 glyphware（d:/work/kirikiri/glyphware、約 3k 行、MIT）

FreeType + HarfBuzz + SheenBidi のフォントエンジン。ホスト非依存（バイト列は `FontLoader` 注入）。

- `Face`: メタデータ、メトリクス（hinted/unhinted）、アウトライン、ビットマップ、**カバレッジマスク**（変形焼き込み）、COLR レイヤー展開、fvar
- `Shaper::shapeRun`: 単一ランの HarfBuzz シェイピング。**`Direction` は LTR / RTL のみ**（TTB が無い）
- `Registry` / `FontQuery`: 名前・スタイル・言語・文字カバレッジで検索。フォント管理はこれで足りる
- `Layout.h`（`layoutLine` / `layoutBlock`）: BiDi＋簡易折返し。JLReq 水準ではない（typeset では使わない）
- ICU 非依存

typeset の**フォント層はこれを直接使う**。richtext の `FontManager` / `FontBackend` 抽象は minikin 用の橋渡しなので不要。

### 1.3 周辺

- `krsc2pdf-rs`（d:/test）: 収録用台本 PDF を出すツール。Typst を組み込んで PDF を出している。
  **台本用途の要件はここから直接取れる**（名前欄の高さ・強調倍率・ヘッダ／フッタ・柱・注記色・抜き台本 等）。
  Typst 依存を外す受け皿にもなり得る（Typst の縦書きは本来サポートが弱い。現状どう縦書きしているかは未確認）。
- `psdparse` / `clipparse`: pybind11 + scikit-build-core の Python バインド前例。**同じ構成に揃える**。

---

## 2. README の要件と足りないもの

| 要件 | 現状 | 必要な追加 |
|---|---|---|
| 縦書き JLReq 組版 | richtext にある | 移植 |
| **横書き JLReq 組版**（小説・技術文書） | 無い（横は minikin ベタ） | インラインエンジンを書字方向非依存にする |
| ルビ・縦中横・圏点・割注・字取り | 縦のみ | 横対応。熟語ルビ、行頭行末での掛かり抑制 |
| 罫線 | 無い | 表示リストに Path、ブロック境界の罫線指定、表罫 |
| ページ処理 | 段組・連結コンテナはある | ページマスタ、柱・ノンブル、見出し・改ページ制御、ウィドウ／オーファン、段抜き、目次 |
| 画像差し込み | 無い | ブロック画像・インライン画像、ラスタ／PDF／SVG への出力 |
| 回り込み | 無い | **行ごとに行長が違う行分割**（`\parshape`）＋排除領域 |
| 漫画の吹き出し | 無い | 形状 → 行ごとの行長関数、天地中央、サイズ自動調整（同じ `\parshape` 機構） |
| 表 | 無い | 固定幅表 → 自動幅は後 |
| SVG 出力 | 無い | 表示リストからパス出力（グリフはアウトライン） |
| PDF 出力 | ある（full embed・無圧縮） | サブセット化・Flate・画像・パス・しおり・リンク |
| Python バインド | 前例あり | pybind11 + scikit-build-core |
| ゲームエンジン組み込み | richtext が担当 | typeset は組版結果（表示リスト）を渡す形。描画は組み込み側でもよい |

---

## 3. 大きな設計判断

### 3.1 richtext との関係 ― 新規に起こして、エンジン部を移植・一般化する（推奨）

| 案 | 内容 | 評価 |
|---|---|---|
| 1. richtext に依存 | typeset は richtext の上に版面・ページ・表・画像を足す | すぐ動くが、minikin（ICU 込み）が付いてくる。横組みが別系統で JLReq 横組みが無い。`GlyphInfo` に `minikin::FontFakery`、`TextStyle` に `minikin::FontCollection` と、minikin の型が API に出ている |
| **2. 新規＋移植** | JLReq コア・注記・PDF・ラスタ・GlyphTransform を **コピーして書字方向非依存に一般化**。フォント層は glyphware 直結。minikin 不要 | 目的（文書組版）に合う形にできる。移植であって再発明ではない。richtext は後で typeset のコアを取り込むことも可能 |
| 3. richtext 内で一般化 | richtext を育てて typeset がそれを使う | richtext はリアルタイム描画ライブラリで責務が違う。膨らむ |

**案 2 を推す。** richtext は「画面に速く出す」、typeset は「文書を正しく組んで出す」で、
最適化の軸が違う（キャッシュ・アトラス vs ページ・Knuth–Plass・PDF 品質）。

### 3.2 ICU を入れない

richtext が ICU を使っていたのは minikin 経由の欧文単語境界だけ。typeset では

- 双方向: SheenBidi（glyphware 内）
- スクリプト・一般カテゴリ・ミラーリング: HarfBuzz の `hb_unicode_*`
- 行分割機会（UAX #14）: **libunibreak**（zlib ライセンス、小さい）。和文は JLReq クラスから直接求まるので、欧文単語内を割らない用途
- UAX #50（縦組みの向き）・East Asian Width・JLReq クラス: 自前テーブル（richtext に UAX #50 相当あり）

必要になったら kirikiri ツリーの最小 ICU（glyphware roadmap 参照）を足せる位置に留める。

### 3.3 単位と座標

- 文書内部は **pt（1/72 in）の float**。richtext の「1px = 1pt」は捨て、ラスタ backend が DPI でスケールする
- 行内は「インライン軸（送り）／ブロック軸（行送り）」の抽象座標で組み、ページに置く時に書字方向で物理座標へ写す
  （richtext の u/v はこれの縦専用版）

### 3.4 中間表現 ― 表示リスト（Display List）

richtext の「グリフ列で切る」を一般化し、レイアウトと backend の間に表示リストを置く。

```
GlyphRun   face, size, [gid, x, y, 2x2 変形]…, 塗り／縁取り, 原文範囲（ToUnicode・SVG 代替テキスト用）
Path       moveTo/lineTo/quadTo/cubicTo/close, fill / stroke（罫線・吹き出し・下線など）
Image      画像参照, 変形, クリップ
Rect       塗り矩形（背景・網かけ）
Group      変形／クリップ／不透明度のスコープ
Meta       リンク、しおり、ページラベル、構造タグ（PDF タグ付けの余地）
```

backend は 3 つ: ラスタ（glyphware マスク合成、richtext から移植）、PDF（richtext から移植・強化）、SVG（新規、グリフはパス化）。
ゲームエンジン組み込み時は表示リストをそのまま渡してエンジン側で描く選択肢も残る。

### 3.5 レイアウトモデル

```
PageSequence   ページマスタ（判型・余白・柱・ノンブル・段数・段間・書字方向）→ ページ列
  Region       段＝流し込み先。排除領域（画像の回り込み）と行ごとの有効行長を持つ
    Flow       ブロック列（段落・見出し・画像・表・罫線・ラベル付き段落）を Region へ順に置く。改ページ・段抜き・ウィドウ／オーファン
      Paragraph → Lines
        Itemizer     スクリプト／フォントフォールバック／正立・横倒しでランに分割（glyphware Registry のカバレッジ）
        Shaper       glyphware（正立は TTB、横組み・横倒しは LTR/RTL）
        ItemBuilder  JLReq クラス・アキ量・注記 → Box / Glue / Penalty（richtext 移植）
        LineBreaker  Greedy / Knuth–Plass、**行ごとの行長を受け取る**（richtext 移植＋拡張）
        LinePlacer   調整比でグルーを確定し、ブロック軸の位置と物理座標へ
```

- 「ラベル付き段落」（ラベル欄＋本文）を 1 つのブロック種として持つ。台本の名前欄、箇条書き、用語集を同じ機構で表現する
- 表は固定幅から。セルは入れ子の Flow。自動幅は後回し
- 行送りモデルは JLReq 流（文字サイズ＋行間の固定ピッチ）を基本にし、欧文・サイズ混在は行内でベースライン調整する。CSS の line-height モデルには寄せない

### 3.6 回り込みと吹き出しは同じ機構

どちらも「行の位置によって使える行長が変わる」問題。`LineBreaker` に行長を 1 つではなく
**行番号 → 行長（＋開始オフセット）の列／関数**で渡せるようにする（TeX の `\parshape`）。

- 回り込み: Region の排除矩形から各行の有効区間を求める
- 吹き出し: 形状（楕円・多角形・パス）を行ピッチで走査して各行の弦長を求める。天地中央揃え、
  収まらなければフォントサイズ／行間を段階的に下げて再試行（外側のループ）。漫画慣習（中央揃え・ぶら下げ無し・仮名詰め）はオプション

Knuth–Plass は行長が行ごとに違っても動く（ノードに行番号を持たせる）。richtext の実装は単一行長なので拡張が要る。

### 3.7 文書入力

- 第一はプログラム API（C++ の構造体ツリー、Python から組む）。ツール用途ならこれで十分
- インラインのマークアップは**任意層**として、richtext の `TagParser` 系タグの組版部分（font / b / i / ruby / color 等）に
  `｜親文字《ルビ》`（青空文庫記法）と縦中横・圏点の指定を足したものを用意する
- CSS エンジンにはしない。スタイルは明示フィールドの構造体、継承は親からのコピー

### 3.8 Python バインド

psdparse / clipparse と同じ **pybind11 + scikit-build-core**。API の当たり:

```python
import jtypeset as ts
doc = ts.Document()
doc.fonts.add("NotoSerifJP-Regular.otf")
master = ts.PageMaster(size=ts.paper.A5, margin=ts.Margin(20*ts.mm),
                       writing_mode="vertical-rl", columns=2, gap=8*ts.mm,
                       header=..., footer=ts.PageNumber())
flow = doc.flow(master)
flow.add(ts.Paragraph("　吾輩は猫である。", style=body))
flow.add(ts.Labeled(label="太郎", body=ts.Paragraph("……")))   # 台本の名前欄
doc.layout()
doc.save_pdf("out.pdf")
doc.save_svg("out-{page}.svg")
img = doc.render(page=0, dpi=144)          # bytes / numpy
lines = ts.fit_text("吹き出しの文", shape=ts.Ellipse(...), style=body)  # 低レベル API も出す
```

低レベル（1 段落を形状に組む、行分割結果を取る）も公開して、吹き出しツールのような用途に使えるようにする。

### 3.9 ライセンス

typeset 本体は MIT。依存: FreeType（FTL）、HarfBuzz（MIT）、SheenBidi（Apache-2.0）、
libunibreak（zlib）、zlib、画像は stb_image（PD/MIT）程度。**Vivliostyle（AGPL）は参照しない**。

---

## 4. 参照する仕様・実装

| 分類 | 参照先 | 何を取るか |
|---|---|---|
| 日本語組版 | **JLReq**（W3C）、JIS X 4051 | 文字クラス・アキ量・禁則・行の調整の優先順位・ルビ・割注・段組・柱ノンブル |
| CSS | Writing Modes 3/4、Text 3/4（text-spacing / hanging-punctuation / line-break）、Ruby、Fragmentation、Page / GCPM、Exclusions / Shapes | 用語と挙動の整理。特に text-spacing と ruby の位置規定、フラグメント（改ページ）の規則 |
| TeX 系 | TeX（Box/Glue/Penalty、`\parshape`）、pTeX JFM、LuaTeX-ja（jfm・kinsoku テーブル） | 既に採用済みのモデルの詳細。行長可変の行分割 |
| 実装 | SILE（MIT、Lua＋HarfBuzz、任意形状フレーム、Knuth–Plass）、Typst（Apache-2.0、Rust、Region/Flow とフラグメント）、Blink LayoutNG / Skia SkParagraph（BSD） | アーキテクチャ・API 設計の参考。krsc2pdf-rs が Typst を使っているので要件の写し取り元にもなる |
| 商用実装の機能表 | Antenna House Formatter、Prince のドキュメント | 日本語組版機能のチェックリストとして |
| PDF | ISO 32000-1（PDF 1.7）、**HarfBuzz hb-subset** | サブセット化は自前で書かず hb-subset（glyf / CFF / CFF2 対応、HarfBuzz は既にリンク済み）を使う |
| SVG | SVG 1.1 / 2 | パス出力。`<text>` は使わず、代替テキストは `aria-label` か `<desc>` |
| Unicode | UAX #14（libunibreak）、UAX #50、EAW、UAX #9（SheenBidi） | |

---

## 5. 進め方（案）

| Phase | 内容 | 完了条件 |
|---|---|---|
| 0 | 骨格: リポジトリ、CMake（vcpkg、glyphware を `add_subdirectory`）、表示リスト、ラスタ／PDF／SVG の最小 backend | 矩形と数グリフを 3 backend に同じ座標で出せる |
| 1 | インライン組版コアの移植・一般化: JLReq クラス／アキ量／アイテム／行分割（行長可変）／注記、Itemizer、glyphware への TTB 追加 | 同じ文を縦と横で組み、約物詰め・禁則・両端揃え・ルビ・縦中横が両方で正しい |
| 2 | ブロック／ページ: PageSequence、Region、Flow、改ページ制御、柱・ノンブル、ラベル付き段落、段抜き見出し | 台本（名前欄＋本文、2 段、柱、ページ番号）と小説本文の PDF が出る |
| 3 | 画像・罫線・回り込み・固定幅表 | 絵入り技術文書（横組み・見出し・図・表・回り込み）が出る |
| 4 | 吹き出し: 形状 → 行長関数、天地中央、サイズ自動調整 | 楕円・多角形の吹き出しにテキストを収めて SVG/ラスタで出す |
| 5 | Python バインド、ツールサンプル（台本・吹き出し・小説）、ドキュメント | pip でビルドでき、上記 3 種のサンプルが Python から動く |
| 6 | PDF 品質: hb-subset、Flate、画像埋め込み、しおり・リンク、ToUnicode。SVG 仕上げ | 和文フォント埋め込みの台本 PDF が数 MB 以下 |

Phase 1〜2 が終わった時点で krsc2pdf の Typst 部分を置き換える検証ができる。

---

## 6. リスク・注意点

- **行長可変の Knuth–Plass**: 行番号依存になるので richtext 版の単純移植では済まない。Greedy を先に通し、KP は後追い
- **CFF サブセット**は自前実装が重い → hb-subset に寄せる。PDF 側は CIDFontType0C（`/FontFile3 /Subtype /CIDFontType0C`）への切替が要る
- 縦メトリクス（vmtx/VORG）の無いフォントの合成は richtext のやり方（1em 送り、原点を ascender 分上へ）を引き継ぐ
- サイズ混在行のブロック軸配置（欧文・ルビ・割注）の規則を早めに決める。ここが曖昧だと PDF と SVG で見た目がずれる
- glyphware の `Shaper` に TTB を足す変更は glyphware 側（kirikiri ツリー）に入る。richtext の `VerticalShaper` が
  「sub_font の font funcs を差し替えると縦メトリクスが取れない」と記録している点に注意
- 表の自動幅、段のバランス取り、熟語ルビは初期スコープから外す

---

## 7. 確認したいこと

1. **richtext との関係**: 案 2（新規＋移植・一般化、minikin 非依存）で進めてよいか
2. **台本ツール**: krsc2pdf-rs の Typst 置き換えを typeset の実用ターゲットに置いてよいか（要件の写し取り元になる）
3. **ICU 非依存**（libunibreak）でよいか。それとも kirikiri ツリーの最小 ICU を最初から使うか
4. **glyphware への変更**（TTB シェイピング、itemization 補助）を typeset の作業に含めてよいか
5. **吹き出しの入力形**: パス／楕円・多角形／マスク画像のどれを想定するか。フォントサイズ自動調整は必要か
6. **表の優先度**: Phase 3 でよいか、もっと後でよいか
7. Python は pybind11（psdparse 準拠）でよいか。nanobind に乗り換える意図があるか

---

## 8. 決定事項（2026-09-08）

1. richtext とは別に新規に起こし、エンジン部を移植・一般化する。**横組みも同じエンジン**で組む（minikin は使わない）
2. 実用ターゲットは **台本（krsc2pdf の Typst 置き換え）・ライトノベル・技術文書**。吹き出しは将来（回り込みに必要な「行ごとの行長」機構は最初から入れる）
3. ICU 非依存（libunibreak + HarfBuzz の Unicode 関数 + SheenBidi + 自前テーブル）
4. glyphware への変更（TTB シェイピング等）は typeset の作業に含める
5. 表は技術文書向けに **TeX の tabular 水準**（列幅指定・罫線・multicolumn・ページまたぎ）を Phase 3 で
6. Python は pybind11 + scikit-build-core（psdparse 準拠）

vcpkg に freetype / harfbuzz 14.2（hb-subset 込み）/ libunibreak 7.0 / zlib / libpng / stb / pybind11 があることを確認済み。
具体設計は `設計.md`。
